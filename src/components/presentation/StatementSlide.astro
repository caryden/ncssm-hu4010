---
import Slide from './Slide.astro';

const { id, title, section } = Astro.props;
---

<Slide id={id} type="statement" title={title} section={section}>
  <div class="slide-content">
    <p class="statement">
      <slot />
    </p>
  </div>
</Slide>
