---
/**
 * ListSlide Component
 *
 * Slide with title and bulleted list. Extends StandardSlide
 * with automatic list wrapper and optional footer slot.
 *
 * @example
 * <ListSlide id="12" title="Key Takeaways">
 *   <li>Start with customers, not products</li>
 *   <li>Segment to find your best opportunity</li>
 *   <li>Pick ONE beachhead to dominate</li>
 * </ListSlide>
 *
 * @example
 * <ListSlide id="28" title="Workshop Time">
 *   <li>Brainstorm segments (10 min)</li>
 *   <li>Score each segment (10 min)</li>
 *   <p slot="footer" style="margin-top: 40px; color: var(--gold);">
 *     Ready? Let's go!
 *   </p>
 * </ListSlide>
 */

import type { ListSlideProps } from './types';
import StandardSlide from './StandardSlide.astro';

interface Props extends ListSlideProps {}

const {
  id,
  title,
  section,
  class: className,
} = Astro.props;
---

<StandardSlide id={id} title={title} section={section} class={className}>
  <ul class="content-list">
    <slot />
  </ul>
  <slot name="footer" />
</StandardSlide>
