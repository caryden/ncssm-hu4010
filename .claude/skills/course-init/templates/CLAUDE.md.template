# Claude Instructions for {COURSE_NAME}

## Project Overview

This repository contains materials for **{COURSE_NAME}** at {INSTITUTION_NAME}. {FRAMEWORK_DESCRIPTION}

**Delivery Context:** Presentations are shown on ~80" TVs in classrooms AND streamed over Zoom. All design decisions must account for both viewing contexts.

---

## RPIV Workflow

This project uses the Research-Plan-Implement-Verify/Validate (RPIV) methodology for curriculum development.

### Quick Start

```bash
/artifact-status     # Check what exists and what's missing
/rpiv-workflow       # Get guided workflow recommendations
```

### Available Skills

| Skill | Purpose |
|-------|---------|
| `/course-research` | Research pedagogical approaches |
| `/course-plan` | Define teaching philosophy |
| `/syllabus-build` | Create session schedule |
| `/master-spec` | Establish design system |
| `/class-research {topic}` | Research a specific topic |
| `/lesson-plan {topic}` | Plan instruction |
| `/presentation-spec {topic}` | Specify slides |
| `/presentation-build {topic}` | Build presentation |
| `/presentation-review {topic}` | Validate and verify |
| `/narration-build {topic}` | Generate voice narration |
| `/artifact-status` | Check completion status |
| `/gate-check {scope}` | Run quality gates |

---

## Critical Specification Files

### 1. `master-presentation-spec.md` (READ FIRST)
The master design specification governing ALL presentations.

### 2. `SYLLABUS.md`
Course schedule, learning objectives, and class topics.

### 3. `course-plan.md`
Teaching philosophy and guiding principles.

### 4. Class-Specific Files
- `class-{n}-{topic}/presentation-spec-{topic}.md` - Slide specifications
- `class-{n}-{topic}/lesson-plan-{topic}.md` - Teaching notes

---

## Minimum Font Size Requirements (CRITICAL)

**Presentations must be readable from the back of a classroom AND over Zoom.**

| Element Type | Minimum Size | Notes |
|--------------|--------------|-------|
| Titles/Headlines | `2.5rem` (40px) | Slide titles, section headers |
| Primary Content | `1.5rem` (24px) | Main text, bullet points |
| Secondary Content | `1.25rem` (20px) | Supporting text |
| Labels & Captions | `1.25rem` (20px) | **Chart labels, D3 text** |
| Absolute Minimum | `1rem` (16px) | Only non-essential metadata |

### Code Review Rules

```javascript
// REJECT - Too small for classroom
.attr('font-size', '12px')
.attr('font-size', '14px')

// ACCEPT - Classroom readable
.attr('font-size', '1.25rem')   // Preferred for labels
.attr('font-size', '1.5rem')    // Body text
```

---

## File Structure

```
{PROJECT_ROOT}/
├── .claude/
│   ├── rpiv-config.json          # RPIV settings
│   └── skills/                   # RPIV skills
├── src/
│   ├── components/presentation/  # Shared components
│   ├── config/
│   │   ├── institution.ts        # Institution branding
│   │   └── course.ts             # Course metadata
│   ├── layouts/
│   │   └── PresentationLayout.astro
│   └── pages/
│       ├── index.astro           # Course homepage
│       └── class-{n}-{topic}/    # Class presentations
├── public/
│   ├── audio/                    # Narration files
│   └── images/                   # Course images
├── shared/
│   ├── styles.css                # Common CSS
│   └── presentation.js           # Navigation logic
├── CLAUDE.md                     # This file
├── SYLLABUS.md                   # Course schedule
├── course-plan.md                # Teaching philosophy
└── master-presentation-spec.md   # Design system
```

---

## Development Commands

```bash
npm run dev      # Start development server
npm run build    # Build for production
npm run preview  # Preview production build
```

---

## Quality Gates

The RPIV workflow includes quality gates at critical transitions:

| Gate | When | Question |
|------|------|----------|
| Gate 1 | After lesson-plan | Does it align with course plan? |
| Gate 2 | After presentation-spec | Does spec achieve objectives? |
| Gate 3 | After presentation-build | Does implementation match spec? |
| Gate 4 | Verification | Did we build it right? |
| Gate 5 | Validation | Did we build the right thing? |

Run `/gate-check {scope}` to validate artifacts.

---

## Course-Specific Notes

{COURSE_SPECIFIC_NOTES}

---

## Narration Configuration

{NARRATION_SECTION}

---

*Generated by RPIV course-init skill*
